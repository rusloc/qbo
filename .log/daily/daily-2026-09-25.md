# daily 2026-09-25 — DAY OPEN

## Done
- Project scaffold: `CLAUDE.md` from `CLAUDE-template.md`; `.docs/` (ADR, ADD, model); `.log/` (coms, daily, plan, weekly, features_and_decisions); `etl/`, `fixtures/`, `demo_data/`, `PNL/`
- pbi-fabric-agent-kit 2026-09-25 installed: 9 skills + `.claude/common`, `.claude/settings.json`, `.mcp.json` (powerbi-modeling, read-write), `_scripts/validate.py`, `scripts/sync-pbi-fabric-skills.ps1`, `_mcp/pg-sqldev` (source only)
- Kit sections merged into `CLAUDE.md`; project entries added to `.gitignore`
- Checks: kit checksums 223/223; `validate.py` GREEN; skill links 916 checked, 17 broken = known upstream set
- Tool setup guide: `.docs/setup-guide.html` (published as a private artifact, v2)
- GitHub repo `git@github.com:rusloc/qbo.git` recorded in `CLAUDE.md`; guide step 5 rewritten (SSH check, remote add, empty vs. non-empty repo push variants)
- Q&A: git vs `gh` vs Git MCP vs GitHub MCP. Recommendation given; nothing installed
- Git: first commit f243c6d (235 files) by USER; `prod` + `dev` pushed to `rusloc/qbo` with tracking; `prod` = GitHub default branch; `CLAUDE.md` branching note updated

- `powerbi-modeling` MCP verified live in VS Code (read-only call); Supabase MCP `sb` found registered for `C:/` only (connected there, absent here)
- Supabase project `vosk.dev` recorded in `CLAUDE.md`, `tech-stack.md`, setup guide (v4)
- Supabase MCP access verified (read-only checks: URL, SELECT, migrations, extensions, security advisors = 0 lints)
- Readiness audit vs spec: root `qbo-pnl-project-spec.md` = identical copy of the canonical spec; tooling inventory done; verdict not ready (see blockers)
- Tenancy rule amended: `vosk.dev` shared with the USER's other backends, QBO in its own schema (`CLAUDE.md`, ADR-0008 accepted, `tech-stack.md`, `features_and_decisions.md`)
- ADR-0002 accepted (dbt for transforms, migrations for DDL); ADR-0003 schema `qbo` accepted, roles / exposure / apply path proposed; ADR-0007 proposed (Supabase Vault)
- Draft SQL init plan: `.docs/ADD/features/f-05-warehouse-init.md` (M1–M4 + M5 pending); not applied

## Blocked / waiting on USER
- Setup guide steps 1–4 and 6–8 (reload + MCP approval, sign-ins, Desktop settings, kit verification, Python 3.12, Intuit app + sandbox, Supabase project + CLI)
- `gh auth login`
- Python 3.12 not installed (3.13 / 3.14 only) → USER reinstalls with 3.12 only #blocker
- Duplicate spec at repo root: delete (USER's call)
- Spec issues flagged (§9.7): `fact_budget` PK with nullable `class_key`; Track C staging shapes undefined; budget source for real clients; §8 CI vs live gate steps; `TOTALYTD` vs `FY_START`; `raw_*` vs `raw_entity`; schema name in PBI parameters #blocker
- Approve: SQL plan M1–M4 + changes vs spec §2; ADR-0003 proposed parts (roles, exposure, MCP `apply_migration` + `ask` rule); ADR-0007 (Vault vs "env vars") #blocker #adr
- Setup guide step 8 (`supabase link` / `db push`) to be reworded once the apply path is decided
- Supabase MCP `supabase` in `.mcp.json` (project-scoped `howp…`): access verified, but read-write as `postgres` → add `read_only=true` (+ drop `functions`)? Old `sb` for `C:/` (unscoped, read-write) still registered
- Choose: GitHub MCP (read-only) / Git MCP with write tools denied / allow read-only `git status|diff|log` / none

## Ideas / Decisions / Features delta
- Decisions: ADR-0001 Supabase Postgres warehouse (accepted); React report as second consumer; kit install defaults; repo = GitHub `rusloc/qbo` (SSH remote `origin`); branch model amended to `prod ← dev` (no feature branches; work on `dev`, regular `--ff-only` merges into `prod`), `prod` default; Supabase project = `vosk.dev`; ADR-0002 dbt (accepted); ADR-0003 schema `qbo` (accepted, rest proposed); ADR-0007 Vault (proposed); `vosk.dev` shared with other backends, QBO in its own schema, "one Supabase project per client" dropped (ADR-0008 accepted)
- ADR candidates: ADR-0002 … ADR-0007
- Ideas: GitHub MCP, read-only, toolsets repos/issues/pull_requests/actions, token via `${GITHUB_PAT}`, once CI exists; Git MCP only with `mcp__git__*` write tools denied
- Features: F-01 … F-08 registered (planned)
