# daily 2026-09-25 — DAY OPEN

## Done
- Project scaffold: `CLAUDE.md` from `CLAUDE-template.md`; `.docs/` (ADR, ADD, model); `.log/` (coms, daily, plan, weekly, features_and_decisions); `etl/`, `fixtures/`, `demo_data/`, `PNL/`
- pbi-fabric-agent-kit 2026-09-25 installed: 9 skills + `.claude/common`, `.claude/settings.json`, `.mcp.json` (powerbi-modeling, read-write), `_scripts/validate.py`, `scripts/sync-pbi-fabric-skills.ps1`, `_mcp/pg-sqldev` (source only)
- Kit sections merged into `CLAUDE.md`; project entries added to `.gitignore`
- Checks: kit checksums 223/223; `validate.py` GREEN; skill links 916 checked, 17 broken = known upstream set
- Tool setup guide `.docs/setup-guide.html`, private artifact; republished through v5 today
- GitHub repo `git@github.com:rusloc/qbo.git` recorded in `CLAUDE.md`; guide step 5 rewritten (SSH check, remote add, empty vs. non-empty repo push variants)
- Q&A: git vs `gh` vs Git MCP vs GitHub MCP. Recommendation given; nothing installed
- Git: first commit f243c6d (235 files) by USER; `prod` + `dev` pushed to `rusloc/qbo` with tracking; `prod` = GitHub default branch; branch model amended to `prod ← dev` (no feature branches)
- MCP: `powerbi-modeling` verified live in VS Code (read-only call). Supabase: USER's first entry `sb` was registered for `C:/` only; USER then added `supabase` to `.mcp.json` (project-scoped `howp…`); access verified with read-only checks (URL, SELECT, migrations, extensions, security advisors = 0 lints)
- `vosk.dev` found shared (another app: 8 migrations, `public` / `demo_sales` / `private`, pg_cron) → tenancy rule amended: QBO in its own schema, "one Supabase project per client" dropped (ADR-0008)
- Readiness audit vs spec: root `qbo-pnl-project-spec.md` was an identical copy of the canonical spec (USER deleted it); 7 spec issues flagged; tooling inventory
- ADRs accepted: ADR-0002 (dbt for transforms, migrations for DDL), ADR-0003 (schema `qbo`; roles `qbo_etl` / `qbo_reader`; no Data API exposure; migrations applied via MCP `apply_migration` with USER approval, verified on `vosk.dev`), ADR-0007 (refresh token in Supabase Vault), ADR-0008
- Draft SQL init plan `.docs/ADD/features/f-05-warehouse-init.md`: M1 schema + roles, M2 raw + control, M3 dims, M4 facts, M5 Vault token functions; changes vs spec §2 listed; rollback noted. Nothing applied
- `CLAUDE.md`: stack, layout (`dbt/`), Warehouse lane, CI step, tenancy, exposure + roles, migration rule (`…_qbo_<name>.sql`, MCP `apply_migration` after approval, `execute_sql` read-only), rule "no Anthropic / Claude references in commit messages"
- `.claude/settings.json`: `ask` rule for `mcp__supabase__apply_migration` and `mcp__supabase__deploy_edge_function`
- Git tree checked by reading `.git/` only: the 3 earlier commit messages are clean (no Claude / Anthropic)
- Python: USER reinstalled with 3.12.10 only; `fab` 1.7.0 moved to 3.12, user PATH on 3.12 `Scripts`; leftover `pythoncore-3.13-64` folder (160 MB, no `python.exe`) deleted by USER; verified after VS Code restart (single `fab` on 3.12, `fab auth status` logged in, no 3.13 / 3.14 references, `validate.py` GREEN on 3.12). Migration complete
- `etl/.venv` (3.12.10) + `etl/requirements.txt`: dbt-core 1.12.5, dbt-postgres 1.11.0, psycopg2-binary 2.9.10, pytest 9.1.1, ruff 0.16.9; `pip check` clean
- `dbt/` scaffold: project `qbo_pnl`, env-only `profiles.yml` (`QBO_DB_*`, `DBT_ENV_SECRET_QBO_DB_PASSWORD`), `models/sources.yml`, empty staging / marts / serve; `dbt parse` exit 0; dbt output gitignored
- Commits by USER, all pushed: 81b5d54, 8f1cab4, 588f82e, 04e1934, e40e2c1 (`dev` = `origin/dev`, read from `.git/`; messages clean); log updates since e40e2c1 not committed yet
- Sub-agents `bip` (Power BI) and `db-chef` (Supabase schema) added by USER in `.claude/agents/`. Not loadable at first (folder created mid-session → restart); after the restart both load. Probes: skill preload works (USER copied `db-schema-architect` + `pg-sql-dev` into `.claude/skills/`), CLAUDE.md is inherited, chef has 10 Supabase tools (8 listed names don't exist on the project-scoped server)
- Agents aligned with CLAUDE.md / ADRs per USER choices (commit e40e2c1): chef = project section (scope `qbo`, spec §2 names, identity keys, `numeric(15,2)`, no RLS per ADR-0003, ADR-0002 dbt contract, migration naming + apply path), memory candidates `SCAND-NNN`, 8 stale Supabase tools dropped, `dax-sql-formatter` preloaded, no `execute_sql`; bip = PBI-only description, delegated mode (main session logs), `coms-{date}.md`, explicit day-close only, spec + `.docs/model/` instead of `context/`, Fabric data engineering dormant, no `style/*` branches, `disallowedTools` Vercel + Supabase `apply_migration` / `deploy_edge_function`. Checks: YAML parses, bytes kept (bip LF, chef CRLF, no BOM). Live re-probe still showed the pre-edit definitions (agents cached at load) → pending restart
- Third sub-agent `data-engineer-ferry` + skill `data-engineer-py` added by USER. Skill first nested one level too deep ("Unknown skill", not preloaded); USER moved it up → loads for the main session and ferry, all 7 references present. Re-probes: bip / chef edits live (bip 0 Vercel tools, no `apply_migration`; chef 3 skills, project section, `SCAND-NNN`)
- dbt ownership decided: ferry owns all dbt work + `etl/`, `fixtures/`, `demo_data/` → ADR-0009 written (accepted: roster, ownership, routing + logging, remote-write limits, memory IDs, open ferry items); `CLAUDE.md` lane table (Agent column, ETL + dbt lane, dbt → migration hand-off rule, shared-metric owner = ETL + dbt lane, `.claude/agents/` in layout); `tech-stack.md` sub-agent row; F-05 lanes split; ferry / chef / bip files name the owner
- CAND-001: Windows Smart App Control blocks a new unsigned native wheel by reputation (psycopg2-binary 2.9.13 `_psycopg` .pyd) → pin an older, widely used release; don't disable SAC (often irreversible without a Windows reset)

## Blocked / waiting on USER
- Migration files M1–M5: all three sub-agents in place → ready for db-chef on USER go; apply (plan step 5) and role logins (step 6) follow
- ferry alignment with CLAUDE.md (ADR-0009 "Open"): `sql-style-formatter` vs `dax-sql-formatter`; `execute_sql` keep / drop; stale tools (`get_logs`, `mcp__microsoft-learn__*`); Polars / pandas vs `Decimal`; `uv` / psycopg 3 vs `etl/.venv` + psycopg2 pin; Airflow / managed ingestion vs ADR-0004; memory `CAND-NNN` #decision
- Optional: add read-only `mcp__supabase__query_logs` to chef (successor of the dropped `get_logs`)
- Approve after explanation: F-05 changes vs spec §2 (+ optional `stmt_section` check); `.mcp.json` → `${SUPABASE_PROJECT_REF}`
- Setup guide steps still open: 3 (Desktop settings), 4 (kit verification, Phase 2), 7 (Intuit app + US sandbox → `etl/.env`), 8 (logins + env, after M1)
- `gh auth login`
- Spec issues flagged (§9.7), not resolved: `fact_budget` PK with nullable `class_key` (fix proposed in F-05); Track C staging shapes; budget source for real clients; §8 CI vs live gate steps; `TOTALYTD` vs `FY_START`; `raw_*` vs `raw_entity`
- Old Supabase MCP `sb` for `C:/` (unscoped, read-write) still registered: remove if unused
- Choose: GitHub MCP (read-only) / Git MCP with write tools denied / allow read-only `git status|diff|log` / none

## Ideas / Decisions / Features delta
- Decisions: ADR-0001 Supabase Postgres warehouse; React report as second consumer; kit install defaults; repo = GitHub `rusloc/qbo` (SSH remote `origin`); branch model `prod ← dev` (no feature branches, `--ff-only` merges), `prod` default; Supabase project `vosk.dev`, shared, QBO in schema `qbo` (ADR-0008); ADR-0002 dbt; ADR-0003 schema, roles, exposure, migration apply path; ADR-0007 Vault; Python 3.12 only on this PC; commit messages without Anthropic / Claude references; sub-agents `bip` (PBI model + Service) and `db-chef` (schema `qbo`, `supabase/`), dbt ownership decided with the data-engineer agent, delegated agents don't write `.log/` (main session logs), chef memory candidates `SCAND-NNN`, no web report agent / skill for now; ADR-0009 sub-agent roster (ferry = ETL + all dbt, chef = migrations, bip = PBI; shared metrics owned by the ETL + dbt lane)
- ADR candidates still open: ADR-0004 (ETL runtime), ADR-0005 (web stack), ADR-0006 (Power BI Service refresh + deploy)
- Anti-pattern candidates: CAND-001 (Smart App Control vs unsigned native wheels)
- Ideas: GitHub MCP, read-only, toolsets repos/issues/pull_requests/actions, token via `${GITHUB_PAT}`, once CI exists; Git MCP only with `mcp__git__*` write tools denied
- Features: F-01 … F-08 registered (planned); F-05 prep done (SQL plan draft, dbt scaffold), state stays planned
