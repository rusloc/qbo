> **DRAFT — projection state.** Sequential plan for the rest of 2026-09-25; order and scope change with USER decisions.

# plan 2026-09-25

## Done so far
- [x] Scaffold project + `CLAUDE.md`
- [x] Install pbi-fabric-agent-kit, merge its CLAUDE sections, verify
- [x] Tool setup guide (HTML)
- [x] Record GitHub repo `rusloc/qbo`; guide step 5 updated
- [x] Explain git / `gh` / Git MCP / GitHub MCP options
- [x] Git repo live: `prod` + `dev` pushed to `rusloc/qbo` (guide step 5, by USER)
- [x] Supabase MCP live (project-scoped, read-write); `vosk.dev` found shared → ADR-0008
- [x] Readiness audit vs spec; 7 spec issues flagged
- [x] ADR-0002 dbt (accepted), ADR-0003 schema `qbo` (accepted in part), ADR-0007 Vault (proposed), F-05 SQL init plan (draft)
- [x] Commit `81b5d54` pushed (USER)

## Rest of today (sequential)
1. ✅ **USER · Python** — reinstall with 3.12 only; move `fab` to 3.12 + PATH; reload VS Code → verify: `py list` = 3.12 only, `fab auth status` OK
2. ✅ **USER · decisions** (a, b, e decided; c, d explained, awaiting approval):
   a. ADR-0003 proposed parts: roles `qbo_etl` / `qbo_reader`, no Data API exposure, apply via MCP `apply_migration` + `ask` rule
   b. ADR-0007: Vault = what "Supabase env vars" meant?
   c. F-05 changes vs spec §2; `check` on `stmt_section` yes / no
   d. `.mcp.json` → `${SUPABASE_PROJECT_REF}`
   e. Test path (no Docker here): apply M1–M4 straight to `vosk.dev` (additive, one transaction per migration; CLAUDE.md testing policy = deploy target) vs. install a local Postgres first
3. ✅ **AI · config** — flip ADR statuses; `.mcp.json` env var (on hold, 2d); `ask` rule in `.claude/settings.json`; CLAUDE.md rule "`execute_sql` read-only" → verify: settings JSON parses, MCP loads after reload
4. ⏸ **AI · migration files** (paused: USER adds sub-agents first) — `supabase/migrations/` M1–M4 (+ M5 if Vault accepted) → verify: match F-05, house style
5. ⏸ **AI + USER · apply** (waits for 4) — M1 → M4, one MCP call each, USER approves each → verify after each: `list_tables`, grants per role, advisors
6. ⏸ **USER · logins** (waits for 5: roles don't exist yet) — `alter role … login password` in the SQL editor; passwords to the password manager; `DB_URL` for `qbo_etl` (session pooler) into `etl/.env`
7. ✅ **AI · Python env + dbt scaffold** (`dbt parse` green; `dbt debug` after 6) — `etl/.venv` (3.12): dbt-core, dbt-postgres, pytest, ruff; `dbt/` project `qbo_pnl` (profiles via `env_var()`, source `qbo.raw_entity`) → verify: `dbt debug` green as `qbo_etl`
8. ✅ **AI · setup guide** (v5) — `fab` on 3.12; Supabase step without `db push`; roles / logins → republish
9. ⏸ **Stretch** (paused by USER) — spec issue 2 (Track C writes QBO-shaped JSON into `raw_entity`) → start `generate_synthetic.py`

## Parallel (USER, any time)
- Track A: Intuit developer account + app + US sandbox company (QBO Plus) → keys into `etl/.env`
- `gh auth login`

## Later (not today)
- ADR-0004 ETL runtime · ADR-0005 web stack · ADR-0006 Power BI Service refresh + deploy
- Spec issues: budget source, CI vs live gates, `TOTALYTD` / `FY_START`
