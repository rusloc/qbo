> **DRAFT — projection state.** Plan for the rest of 2026-09-25, aligned with `.log/daily/daily-2026-09-25.md` (DAY OPEN). Order and scope change with USER decisions.

# plan 2026-09-25

## Done today (detail in the daily)
- [x] Scaffold, pbi-fabric-agent-kit, setup guide v5, GitHub repo `rusloc/qbo` (`prod` ← `dev`)
- [x] Supabase MCP live; `vosk.dev` shared → QBO in schema `qbo` (ADR-0008)
- [x] Readiness audit vs spec; spec issues flagged
- [x] ADRs accepted: 0002 dbt, 0003 schema / roles / apply path, 0004 Windows Task Scheduler, 0007 Vault (amended: advisory lock), 0008 shared project, 0009 sub-agent roster
- [x] Python 3.12 only; `etl/.venv` + pinned `etl/requirements.txt` (dbt, psycopg2 2.9.10, psycopg 3.3.6, DuckDB 1.5.5, pytest, ruff); `dbt/` scaffold (`dbt parse` OK)
- [x] Sub-agents `bip`, `db-chef`, `data-engineer-ferry` added and aligned with CLAUDE.md; skill `data-engineer-py` loads
- [x] Build gate: F-05 changes vs spec §2 + `stmt_section` check approved; demo data = QBO-shaped JSON; Power BI deferred
- [x] Warehouse live: M1–M5 applied to `vosk.dev` and verified; `qbo_etl` / `qbo_reader` can log in (USER)
- [x] `validate.py` GREEN; commits pushed up to c799763

## Next (sequential)
1. ⏳ **USER · `etl/.env`** — database lines + Intuit keys, as explained 2026-09-25; values never in chat → verify: file exists (listing only)
2. ⏳ **USER · restart Claude Code** → AI re-probes ferry → verify: preloads `data-engineer-py` + `dax-sql-formatter`; 3 Supabase tools
3. ⏸ **ferry · env loader + `dbt debug`** (waits for 1–2) → verify: `dbt debug` green as `qbo_etl`; a missing variable is reported by name only
4. ⏸ **ferry · `qbo_sync auth`** (waits for 3; USER approves access in the browser) → verify: secret `qbo_refresh_token` exists in Vault (checked by name only, value never read)
5. ⏸ **ferry · backfill + fixtures (P1, Track A)** → verify: `raw_entity` row counts per entity = API counts; fixtures scrubbed of realmId and tokens
6. ⏸ **ferry · `generate_synthetic.py` (Track C)** — QBO-shaped JSON into `raw_entity`, 24 months, deterministic seed → verify: a re-run gives identical rows
7. ⏸ **ferry · dbt models (P2)** — `stg_*` → dims / `fact_gl` → `vw_*`; one test per `DetailType`; db-chef notes (`dim_date` merge key `date_key`, `fact_budget` null `class_key`) → verify: gate P2 (tests pass, zero orphan `account_key`, two runs = same counts)
8. ⏸ **ferry · `seed_sandbox.py` (Track B)** — POSTs to the QBO sandbox = remote write, USER approves first → verify: idempotent re-run (check-before-create by DocNumber)
9. ⏸ **Validation V1–V3 (P3)** — after 7–8 → verify: 24/24 months reconcile to $0.01

## Parallel (USER, any time)
- Merge `dev` → `prod`: checkpoint reached (warehouse live, `validate.py` GREEN); commands in CLAUDE.md
- `gh auth login`
- Decide `.mcp.json` → `${SUPABASE_PROJECT_REF}`; remove the old `sb` MCP if unused
- Setup guide refresh v6 on request (DuckDB, psycopg 3, Task Scheduler, `etl/.env` template, logins done)

## Later (not today)
- Task Scheduler job (ADR-0004) once `qbo_sync cdc` exists
- ADR-0005 web stack · ADR-0006 Power BI Service refresh + deploy
- Fresh-DB migration check in CI (skipped for now, option D)
- Spec issues: budget source, CI vs live gates, `TOTALYTD` / `FY_START`, `raw_*` vs `raw_entity`
- Power BI (Phase 4): deferred by USER · web report agent / skill: skipped by USER
